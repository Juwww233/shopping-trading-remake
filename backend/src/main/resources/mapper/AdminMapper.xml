<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.shopping3.mapper.AdminMapper">

    <resultMap id="AdminResultMap" type="Admin">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="avatar" property="avatar"/>
        <result column="role" property="role"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
    </resultMap>

    <select id="selectByUsername" resultMap="AdminResultMap">
        SELECT * FROM admin WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="AdminResultMap">
        SELECT * FROM admin WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="AdminResultMap">
        SELECT * FROM admin ORDER BY id
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO admin (username, password, name, avatar, role, phone, email)
        VALUES (#{username}, #{password}, #{name}, #{avatar}, #{role}, #{phone}, #{email})
    </insert>

    <update id="update">
        UPDATE admin
        SET name = #{name}, avatar = #{avatar}, role = #{role}, phone = #{phone}, email = #{email}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM admin WHERE id = #{id}
    </delete>

</mapper>
