<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.shopping3.mapper.GoodsMapper">
    <!-- 猜你喜欢：随机查询6条【审核通过+已上架】的非二手商品 -->
    <select id="selectGuessYouLike" resultType="com.example.shopping3.entity.Goods">
        SELECT * FROM goods
        WHERE category != '二手物品'
          AND status = '审核通过'
          AND sale_status = '已上架'
        ORDER BY RAND()
            LIMIT 6
    </select>

    <!-- 二手好物：查询6条【审核通过+已上架】的二手物品 -->
    <select id="selectSecondHandGoods" resultType="com.example.shopping3.entity.Goods">
        SELECT * FROM goods
        WHERE category = '二手物品'
          AND status = '审核通过'
          AND sale_status = '已上架'
        ORDER BY date DESC
            LIMIT 6
    </select>

    <!-- 新增：按分类查询【审核通过+已上架】的商品 -->
    <select id="selectGoodsByCategory" parameterType="String" resultType="com.example.shopping3.entity.Goods">
        SELECT * FROM goods
        WHERE category = #{category}
          AND status = '审核通过'
          AND sale_status = '已上架'
        ORDER BY date DESC
    </select>

    <select id="selectById" parameterType="Integer" resultType="com.example.shopping3.entity.Goods">
        SELECT * FROM goods WHERE id = #{id}
    </select>

    <!-- 增加浏览量 -->
    <update id="incrementReadCount">
        UPDATE goods SET read_count = read_count + 1 WHERE id = #{id}
    </update>

    <!-- 查询所有商品 -->
    <select id="selectAllGoods" resultType="com.example.shopping3.entity.Goods">
        SELECT * FROM goods
    </select>

    <!-- 更新商品库存 -->
    <update id="updateStock" parameterType="map">
        UPDATE goods SET stock = stock - #{count} WHERE id = #{goodsId}
    </update>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO goods (name, price, stock, content, address, img, date, status, category, user_id, sale_status, read_count)
        VALUES (#{name}, #{price}, #{stock}, #{content}, #{address}, #{img}, #{date}, #{status}, #{category}, #{userId}, #{saleStatus}, #{readCount})
    </insert>

    <update id="update">
        UPDATE goods SET name = #{name}, price = #{price}, stock = #{stock},
        content = #{content}, address = #{address}, img = #{img},
        category = #{category}, sale_status = #{saleStatus}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE goods SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updateSaleStatus">
        UPDATE goods SET sale_status = #{saleStatus} WHERE id = #{id}
    </update>

    <select id="selectByUserId" resultType="com.example.shopping3.entity.Goods">
        SELECT * FROM goods WHERE user_id = #{userId} ORDER BY date DESC
    </select>

    <select id="selectByKeyword" resultType="com.example.shopping3.entity.Goods">
        SELECT * FROM goods WHERE status = '审核通过' AND sale_status = '已上架'
        AND (name LIKE CONCAT('%', #{keyword}, '%') OR content LIKE CONCAT('%', #{keyword}, '%'))
        ORDER BY date DESC
    </select>
</mapper>