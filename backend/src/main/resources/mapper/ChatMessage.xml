<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shopping3.mapper.ChatMessageMapper">

    <insert id="insertMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (session_id, sender_id, content)
        VALUES (#{sessionId}, #{senderId}, #{content})
    </insert>

    <select id="selectMessagesBySessionId" resultType="com.example.shopping3.entity.ChatMessage">
        SELECT * FROM chat_message
        WHERE session_id = #{sessionId}
        ORDER BY created_at ASC
    </select>
</mapper>